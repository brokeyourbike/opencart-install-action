{
  "name": "opencart-install-action",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "test": "vitest run --coverage",
    "format": "prettier --write '**/*.ts'"
  },
  "dependencies": {
    "@actions/core": "^2",
    "@actions/exec": "^2",
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@vercel/ncc": "^0.38.4",
    "@vitest/coverage-v8": "^4.1.5",
    "prettier": "^3.8.3",
    "typescript": "^5",
    "vitest": "^4.1.5"
  }
}
